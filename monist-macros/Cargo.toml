[package]
name = "monist-macros"
version = "0.1.0"
edition = "2021"
description = "Hardware abstraction and procedural macros for Monist graph execution"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full", "extra-traits"] }
