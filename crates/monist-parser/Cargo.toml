[package]
name = "monist-parser"
version = "0.1.0"
edition = "2024"

[dependencies]
monist-core = { version = "0.1.0", path = "../monist-core" }
serde = { version = "1.0.228", features = ["derive"] }
